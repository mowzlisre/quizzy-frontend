import { Box, Flex, Text, VStack, useColorModeValue } from '@chakra-ui/react';
import { LineChart, Line, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer, BarChart, Bar, RadialBarChart, RadialBar, Legend, PieChart, Pie, Cell } from 'recharts';
import { studyTrends, projectStudyDistribution, aggregatedMarks, projectMarksDistribution, assessmentCount } from '../data';

function Dashboard() {
  return (
    <Box p={4} >
    </Box>
  );
}

export default Dashboard;
